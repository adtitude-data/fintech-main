/*
 * AdminClientsPage Messages
 *
 * This contains all the text for the AdminClientsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AdminClientsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AdminClientsPage container!',
  },
});
