/*
 * ClientAccountsPage Messages
 *
 * This contains all the text for the ClientAccountsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ClientAccountsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ClientAccountsPage container!',
  },
});
