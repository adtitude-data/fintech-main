/*
 * AgentClientsPage Messages
 *
 * This contains all the text for the AgentClientsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AgentClientsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AgentClientsPage container!',
  },
});
