/*
 * TwoFaPage Messages
 *
 * This contains all the text for the TwoFaPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TwoFaPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TwoFaPage container!',
  },
});
